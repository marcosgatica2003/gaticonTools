# El gaticon kernel
El kernel gaticon está diseñado para la miniBestia (Celeron N4020 - 8GB de RAM - 256 GB M.2 SATA) aunque puede compilarse para otras computadoras.

[![GPLv3 License](https://img.shields.io/badge/License-GPL%20v3-yellow.svg)](https://opensource.org/licenses/)

> [!NOTE]
> Basado en el kernel Linux LTS 6.12.34

## Construir desde la fuente

### Dependencias
Para Debian:

```
$ apt install build-essential libncurses-dev bison flex libssl-dev libelf-dev libudev-dev libpci-dev libiberty-dev fakeroot cpio bc git
```

Para Arch Linux:

```
$ pacman -S base-devel git ncurses libelf pahole cpio perl tar xz bc flex bison openssl zstd
```
